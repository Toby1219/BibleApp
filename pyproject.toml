[project]
name = "bible-webapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi-cache2>=0.2.1",
    "httpx2>=2.11.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "redis>=8.1.0",
    "setuptools>=84.0.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    
]
