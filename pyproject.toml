[project]
name = "bible-webapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx2>=2.11.0",
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    
]
